import PriorityBadge from '../common/PriorityBadge'; import StatusBadge from '../common/StatusBadge'; export default function IncidentDetails({incident}){if(!incident)return <div className="p-6 text-[#7E8A9A] text-sm">Select an incident to inspect details.</div>;return <div className="p-4 space-y-3"><div className="flex justify-between"><div><div className="font-bold">{incident.id}</div><div className="text-xs text-[#7E8A9A]">{incident.time}</div></div><PriorityBadge priority={incident.priority}/></div><div><div className="cad-label">Complaint</div><p className="text-sm text-[#AAB4C3]">{incident.chiefComplaint || incident.description}</p></div>{incident.narrative&&<div><div className="cad-label">Narrative</div><p className="text-xs leading-relaxed text-[#AAB4C3]">{incident.narrative}</p></div>}<div><div className="cad-label">Location</div><p className="text-sm">{incident.location}</p></div>{incident.caller&&<div className="text-xs text-[#AAB4C3]"><span className="text-[#7E8A9A]">Caller:</span> {incident.caller}{incident.callback&&<> · {incident.callback}</>}</div>}<div><div className="cad-label">Status</div><StatusBadge status={incident.status}/></div>{incident.assignedUnit&&<div><div className="cad-label">Assigned Unit</div><p className="font-semibold text-[#38BDF8]">{incident.assignedUnit}</p></div>}{incident.notes?.length>0&&<div><div className="cad-label">Incident Notes</div><div className="space-y-1">{incident.notes.map((note,index)=><p key={`${note.at}-${index}`} className="border-l-2 border-[#38BDF8]/50 pl-2 text-[11px] text-[#AAB4C3]"><span className="text-[#7E8A9A]">{note.at}</span> · {note.text}</p>)}</div></div>}</div>}
